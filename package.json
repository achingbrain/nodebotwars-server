{
  "name": "nodebotwars-server",
  "version": "0.0.1",
  "scripts": {
    "test": "istanbul cover _mocha",
    "coveralls": "istanbul cover _mocha && cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "rc": "^0.3",
    "dnode": "^1.0",
    "node-uuid": "^1.4"
  },
  "devDependencies": {
    "mocha": "^1.0",
    "chai": "^1.9",
    "sinon": "^1.8",
    "istanbul": "^0.2",
    "coveralls": "^2.8",
    "proxyquire": "^0.5"
  }
}